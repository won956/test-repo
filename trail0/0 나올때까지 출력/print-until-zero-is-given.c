#include <stdio.h>

int main() {
    // Please write your code here.

    int n;

    while(1) {
        scanf("%d", &n);
            if(n == 0) {
                break;
            }

            printf("%d\n", n);
    }
    return 0;
}